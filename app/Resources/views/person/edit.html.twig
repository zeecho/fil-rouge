{% extends 'base.html.twig' %}

{% block body %}
    <h1>Person edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    {% include ':buttons:edit.html.twig' %}
    {{ form_end(edit_form) }}

    <a href="{{ path('person_show', { id: person.id }) }}"><button class="btn btn-default">Annuler</button></a>
    {% include ':form_parts:delete.html.twig' %}

{% endblock %}
