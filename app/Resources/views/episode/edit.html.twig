{% extends 'base.html.twig' %}

{% block body %}
    <h1>Episode edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    {% include ':buttons:edit.html.twig' %}
    {{ form_end(edit_form) }}

    <a href="{{ path('episode_show', { id: episode.id }) }}"><button class="btn btn-default">Annuler</button></a>
    {{ form_start(delete_form) }}
        <input class="btn btn-default" type="submit" value="Supprimer">
    {{ form_end(delete_form) }}
{% endblock %}
