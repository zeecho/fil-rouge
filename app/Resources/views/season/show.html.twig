{% extends 'base.html.twig' %}

{% block body %}
    <h1><a href="{{ path('serie_show', { id: season.serie.id }) }}">{{ season.serie.name }}</a></h1>
    <h2>Saison {{ season.number }}</h2>

    <h4>Liste des épisodes</h4>
    <ul>
        {% for episode in episodes %}
            <li><a href="{{ path('episode_show', { id: episode.id }) }}">{{ episode.name }}</a></li>
        {% endfor %}
    </ul>

    <a href="{{ path('season_edit', { 'id': season.id }) }}">Edit</a>
    {{ form_start(delete_form) }}
    <input type="submit" class="btn" value="Delete">
    {{ form_end(delete_form) }}
{% endblock %}
